import {DialogContent} from "@mui/material";

function InputDialogTemplateContent({
    content
                                    }) {
    return (
        <DialogContent dividers>
            {content}
        </DialogContent>
    )
}

export default InputDialogTemplateContent;
