import {DialogContent} from "@mui/material";

function DialogTemplateContent({
                                   content
                               }) {
    return (
        <DialogContent dividers>
            {content}
        </DialogContent>
    )
}

export default DialogTemplateContent;
